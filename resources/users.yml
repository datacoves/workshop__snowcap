# users.yml

vars:
  - name: users
    type: list
    default:
      - name: testuser
        type: PERSON

role_grants:
  - to_user: testuser
    roles:
      - analyst

  - to_user: fmercado
    roles:
      - ACCOUNTADMIN
      - analyst